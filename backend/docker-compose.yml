services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: softwaredesign-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      LOGGING_LEVEL_FI_TUNI_SOFTWAREDESIGN: INFO
    restart: unless-stopped